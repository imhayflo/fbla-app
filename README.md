# FBLA Member App

Flutter app for FBLA chapter members. Has a dashboard, events calendar, announcements feed, competition tracker, and member profiles.

## Setup

Make sure you have Flutter installed, then:

```
flutter pub get
flutter run
```

## Build

```
flutter build apk --release
flutter build ios --release
```
